WEB_PORT=8000
ENV=dev
# DB
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=appdb
DB_PORT=5432
DATABASE_URL=postgresql://postgres:postgres@db:5432/appdb
# Redis
REDIS_URL=redis://redis:6379/0
REDIS_PORT=6379

# where the user’s browser should land after successful login
FRONTEND_URL=http://localhost:5173

# the fully-qualified public URL of your backend (used to build the email link)
BACKEND_PUBLIC_URL=http://localhost:8000

# JWT secret for session cookie
JWT_SECRET=dev-change-me

# Token TTLs (seconds)
MAGIC_TOKEN_TTL=900        # 15 minutes for magic link
SESSION_TTL_DAYS=30

# SMTP settings (use Mailtrap, SES, or your SMTP)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=587
SMTP_USER=your_smtp_user
SMTP_PASSWORD=your_smtp_password
EMAIL_FROM=no-reply@yourapp.local

FLASK_APP=app.wsgi:app
FLASK_ENV=development

EMAIL_MODE=smtp
SMTP_HOST=mailhog
SMTP_PORT=1025
EMAIL_SENDER=noreply@example.com
SMTP_TLS=false

